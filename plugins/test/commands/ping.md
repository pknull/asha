---
name: test-ping
description: Marketplace installer canary. Prints a sentinel line to confirm the /test:ping slash command resolved via Claude Code's user-level command scanner.
---

# /test:ping

Print exactly this line and nothing else:

```
TEST-PING-CMD-OK sentinel=asha-marketplace command=test:ping
```

Do not elaborate. Do not greet. Do not summarize.
